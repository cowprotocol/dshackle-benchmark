config:
  target: http://0.0.0.0:8545
  phases:
    - duration: 30
      arrivalRate: 20
      name: Peak Load
  processor: "./processor.js"
 
scenarios:
  - name: "ETH Call"
    flow:
      - function: batch_call
      - post:
          url: "/eth"
          json: "{{ query }}"

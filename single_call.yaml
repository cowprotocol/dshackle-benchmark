config:
  target: http://0.0.0.0:8545
  phases:
    - duration: 30
      arrivalRate: 20
  processor: "./processor.js"
 
scenarios:
  - name: "ETH Call"
    flow:
      - function: single_call
      - loop:
        - post:
            url: "/eth"
            json: "{{ query }}"
        count: 50
